# 博客系统使用指南

## 🚀 启动博客

双击运行 **`启动.bat`** 文件

## 🌐 访问地址

启动成功后：

- **前台首页**: http://localhost:5173
- **后台管理**: http://localhost:5173/admin

## 🔑 默认账户

```
用户名：admin
密码：admin123
```

⚠️ 首次登录后请及时修改密码！

## 📝 使用流程

### 1. 发布文章

1. 登录后台：http://localhost:5173/login
2. 点击"文章管理"
3. 点击"新建文章"
4. 填写标题、内容（支持 Markdown）
5. 勾选"发布"
6. 点击"保存"

### 2. 管理分类

1. 进入后台，点击"分类管理"
2. 添加、编辑或删除分类
3. 默认分类：技术、生活、随笔

### 3. 管理评论

1. 进入后台，点击"评论管理"
2. 审核或删除用户评论

## 🎨 自定义样式

### 修改主题色

编辑 `frontend/tailwind.config.js`：

```javascript
theme: {
  extend: {
    colors: {
      primary: {
        // 修改这里的颜色值
        600: '#0ea5e9',
        700: '#0369a1',
      },
    },
  },
}
```

### 修改布局

编辑 `frontend/src/components/Layout.tsx`

## 🔧 常见问题

### 端口被占用

修改 `.env` 文件：

```env
PORT=3001
```

### 数据库重置

删除 `backend/blog.db` 后重新运行：

```bash
npm run init:db
```

### 依赖问题

```bash
npm cache clean --force
npm install
```

## 📁 项目结构

```
boker/
├── backend/              # 后端代码
│   ├── server.js        # 主服务器
│   ├── db.js            # 数据库配置
│   ├── routes/          # API 路由
│   └── middleware/      # 中间件
├── frontend/            # 前端代码
│   └── src/             # 源代码
├── 启动.bat             # 启动脚本
├── package.json         # 项目配置
└── README.md            # 说明文档
```

## 🎯 API 接口

### 文章
- `GET /api/articles` - 获取文章列表
- `GET /api/articles/:id` - 获取文章详情
- `POST /api/articles` - 创建文章（需登录）
- `PUT /api/articles/:id` - 更新文章（需登录）
- `DELETE /api/articles/:id` - 删除文章（需登录）

### 分类
- `GET /api/categories` - 获取分类列表
- `POST /api/categories` - 创建分类（需登录）
- `PUT /api/categories/:id` - 更新分类（需登录）
- `DELETE /api/categories/:id` - 删除分类（需登录）

### 评论
- `GET /api/comments/article/:articleId` - 获取文章评论
- `POST /api/comments/article/:articleId` - 创建评论
- `GET /api/comments/admin/all` - 获取所有评论（需登录）
- `PUT /api/comments/:id/approve` - 审核评论（需登录）
- `DELETE /api/comments/:id` - 删除评论（需登录）

## 💡 提示

1. 文章支持 Markdown 格式
2. 代码块自动高亮
3. 评论需要审核后才能显示
4. 支持文章分类筛选
5. 自动保存草稿功能

---

祝你写作愉快！📝
